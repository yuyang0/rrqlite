pub mod command {
    include!(concat!(env!("OUT_DIR"), "/command.rs"));
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
