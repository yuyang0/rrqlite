mod api;
mod controller;
pub use api::create_server;
pub use controller::Controller;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
